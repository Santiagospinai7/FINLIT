<div id="<%= dom_id saving %>">
  <% if @achive %>
    <div class="intro-accounts-card">
        <%= cl_image_tag Icon.find(saving.icon_id).url, class:"bank-avatar-accounts"%>
        <p><strong>Saving:</strong><br><%= saving.saving_name %></p>
    </div>
    <div>
    <p>Balance: <%= "£#{number_with_precision(saving.balance, precision: 2, delimiter: ',')}" %></p>
    <p><strong>Your already achieve your saving goal, congrats! 🤑🍾</strong></p>
    </div>
  <% else %>
    <div class="intro-accounts-card">
        <%= cl_image_tag Icon.find(saving.icon_id).url, class:"bank-avatar-accounts"%>
        <p><strong>Saving:</strong><br><%= saving.saving_name %></p>
    </div>
    <div>
      <p>Balance: <%= "£#{number_with_precision(saving.balance, precision: 2, delimiter: ',')}" %></p>
      <p>Goal: <%= "£#{number_with_precision(saving.saving_goal, precision: 2, delimiter: ',')}" %></p>
      <p>Saved: <%= Saving.get_porcentage(saving) %>%</p>
    </div>
  <% end %>
</div>

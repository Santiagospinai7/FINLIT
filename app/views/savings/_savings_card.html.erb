<div class="account-grid-item">
  <div class="default-saving-card">
    <p class="default-saving-card-text">Set a goal for yourself in a specific timeframe and easily track your progress towards it. Whether you're saving up for a vacation, or just building your emergency fund, contribute to your goal whenever you have some extra funds available.</p>
  </div>
  <button class="card-action-buttons">Get Started</button>
</div>
<%= link_to saving, class:"account-grid-item" do%>
  <div class="intro-accounts-card">
      <%= cl_image_tag Icon.find(saving.icon_id).url, class:"bank-avatar-accounts"%>
      <p><strong>Savings:</strong><br><%= saving.saving_name %></p>
  </div>
  <div class="savings-card-data">
    <div id="savings-card-data-chart">
      <%= pie_chart Saving.saving_chart(saving), donut: true, height: '100px', width: '100px', legend: false, colors: ["#794CF0", "#4C4B4E"], library: {animation: {easing: 'easeOutQuad'}, elements: {arc: {borderWidth: 0}}, plotOptions: { pie: { dataLabels: { enabled: false }}} } %>
    </div>
    <p>Saved<br><span class="account-card-balance"><%= Saving.get_porcentage(saving) %>%</span></p>
  </div>
  <div class="savings-card-data">
    <div class="savings-message-card">
      <% saving.updateBalance %>
      <p>You've saved <span class="motivational-message-span"><%= "£#{number_with_delimiter(saving.balance, :delimiter => ',')}" %></span><br>Keep it up 🙌</p>
    </div>
  </div>
<% end %>
